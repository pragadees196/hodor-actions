name: testing
on:
  push:
    branches:
      - main
    paths-ignore:
      - k8s/deployment.yaml
  pull_request:
    types:
      - closed
    branches:
      - main
  workflow_dispatch:
jobs:
  tetsing:
    runs-on: ubuntu-latest
    steps:
      - name: testing
        run: echo "Hi this is test"
  